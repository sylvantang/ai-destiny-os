{
  "name": "ai-destiny-os-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build:engine && next dev",
    "build": "npm run build:engine && next build",
    "start": "next start",
    "build:engine": "cd .. && npx tsc",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
